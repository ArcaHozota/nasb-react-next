// src/theme.ts
import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: { main: "#800020" },
    secondary: { main: "#002fa7" },
    success: { main: "#006400" },
    warning: { main: "#f6c91c" },
    info: { main: "#353542" },
  },
});
